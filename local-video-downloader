---
name: local-video-downloader
description: 创建、配置、修复或使用 Windows 本地视频下载器。适用于用户希望通过桌面快捷方式或本地网页粘贴已获授权的视频链接，并用 yt-dlp 与 FFmpeg 下载为 MP4 的场景。
---

# 本地视频下载器

创建一个无需安装 Python 的 Windows 便携版视频下载工具。工具由本地网页、PowerShell 服务、yt-dlp 和 FFmpeg 组成。

## 使用范围

仅处理用户拥有版权、已获得下载授权，或网站明确允许下载的内容。

不得承诺或尝试绕过：

- 付费墙或订阅限制
- 登录验证
- DRM 或其他技术保护措施
- 网站访问控制或版权限制

## 默认交付内容

在用户指定的输出文件夹创建：

- `index.html`：粘贴链接、选择清晰度、显示下载状态的中文网页。
- `server.ps1`：仅监听 `127.0.0.1` 的本地服务，调用 yt-dlp 下载。
- `start.cmd`：启动服务并打开网页。
- `yt-dlp.exe`：Windows 便携版下载程序。
- `ffmpeg.exe` 与 `ffprobe.exe`：用于合并视频和音频。
- `downloads/`：下载结果保存位置。
- 桌面快捷方式：目标设为 `start.cmd`，名称为“视频下载器”。

## 工作流程

1. 确认用户需要本地工具，且明确同意安装或下载便携版程序。
2. 创建网页，包含链接输入框、清晰度选择和下载按钮。
3. 创建仅监听本机地址的 PowerShell 服务。
4. 优先使用工具文件夹内的 `yt-dlp.exe`。
5. 调用 yt-dlp 时传入：
   - `--no-playlist`
   - `--merge-output-format mp4`
   - `--ffmpeg-location <工具文件夹>`
   - `-P <工具文件夹>\downloads`
6. 下载 yt-dlp 官方 Windows 发布程序，以及 FFmpeg 官方下载页推荐的 Windows 构建。
7. 验证：
   - `yt-dlp.exe --version`
   - `ffmpeg.exe -version`
   - PowerShell 脚本语法。
8. 如用户希望方便启动，在桌面创建指向 `start.cmd` 的快捷方式。

## 交互与故障处理

- 用户无需寻找 `start.cmd`：双击桌面的“视频下载器”快捷方式即可。
- 下载时黑色命令窗口必须保持打开。
- 若下载失败，网页应显示 yt-dlp 返回的简短错误信息。
- 不要将服务暴露到局域网或公网；仅使用 `127.0.0.1`。
- 不要删除用户已有文件；清理时只移除本次创建的临时压缩包和解压目录。
- 下载完成后说明视频位于 `downloads` 文件夹。
